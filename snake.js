const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");
const canvasSize = 500;

ctx.fillStyle = "red";
ctx.fillRect(0, 0, 150, 75);

for(let i = 0; i < 500/15; i++) {
  for (let j = 0; j < 500/15; i++) {
    //ctx.fillRect(0, )
  }
}
